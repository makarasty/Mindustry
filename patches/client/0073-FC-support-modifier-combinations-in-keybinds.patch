From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: DeterMination-Wind <DeterMination-Wind@users.noreply.github.com>
Date: Sat, 21 Feb 2026 14:07:41 +0800
Subject: [PATCH] FC: support modifier combinations in keybinds

---
 core/src/mindustry/input/DesktopInput.java    | 19 ++++----
 .../mindustry/ui/dialogs/KeybindDialog.java   | 44 +++++++++++++++----
 2 files changed, 45 insertions(+), 18 deletions(-)

diff --git a/core/src/mindustry/input/DesktopInput.java b/core/src/mindustry/input/DesktopInput.java
index 5c59383e07b7ab77b24218bf6b73a3102d68f199..60dd12fef79ef72da19ae874ff1a02cae615bafb 100644
--- a/core/src/mindustry/input/DesktopInput.java
+++ b/core/src/mindustry/input/DesktopInput.java
@@ -28,6 +28,7 @@ import static arc.Core.*;
 import static mindustry.Vars.net;
 import static mindustry.Vars.*;
 import static mindustry.input.PlaceMode.*;
+import static mindustry.ui.dialogs.KeybindDialog.keyName;
 
 public class DesktopInput extends InputHandler{
     public Vec2 movement = new Vec2();
@@ -94,14 +95,14 @@ public class DesktopInput extends InputHandler{
                     if(!showHint()) return str;
                     str.setLength(0);
                     if(!isBuilding && !Core.settings.getBool("buildautopause") && !player.unit().isBuilding()){
-                        str.append(Core.bundle.format("enablebuilding", Binding.pauseBuilding.value.key.toString()));
+                        str.append(Core.bundle.format("enablebuilding", keyName(Binding.pauseBuilding.value)));
                     }else if(player.unit().isBuilding()){
-                        str.append(Core.bundle.format(isBuilding ? "pausebuilding" : "resumebuilding", Binding.pauseBuilding.value.key.toString()))
-                            .append("\n").append(Core.bundle.format("cancelbuilding", Binding.clearBuilding.value.key.toString()))
-                            .append("\n").append(Core.bundle.format("selectschematic", Binding.schematicSelect.value.key.toString()));
+                        str.append(Core.bundle.format(isBuilding ? "pausebuilding" : "resumebuilding", keyName(Binding.pauseBuilding.value)))
+                        .append("\n").append(Core.bundle.format("cancelbuilding", keyName(Binding.clearBuilding.value)))
+                        .append("\n").append(Core.bundle.format("selectschematic", keyName(Binding.schematicSelect.value)));
                     }
                     if(!player.dead() && !player.unit().spawnedByCore()){
-                        str.append(str.length() != 0 ? "\n" : "").append(Core.bundle.format("respawn", Binding.respawn.value.key.toString()));
+                        str.append(str.length() != 0 ? "\n" : "").append(Core.bundle.format("respawn", keyName(Binding.respawn.value)));
                     }
                     return str;
                 }).style(Styles.outlineLabel);
@@ -115,8 +116,8 @@ public class DesktopInput extends InputHandler{
             t.table(Styles.black6, b -> {
                 b.defaults().left();
                 b.label(() -> Core.bundle.format("schematic.flip",
-                    Binding.schematicFlipX.value.key.toString(),
-                    Binding.schematicFlipY.value.key.toString())).style(Styles.outlineLabel).visible(() -> Core.settings.getBool("hints"));
+                keyName(Binding.schematicFlipX.value),
+                keyName(Binding.schematicFlipY.value))).style(Styles.outlineLabel).visible(() -> Core.settings.getBool("hints"));
                 b.row();
                 b.table(a -> {
                     a.button("@schematic.add", Icon.save, this::showSchematicSave).colspan(2).size(250f, 50f).disabled(f -> lastSchematic == null || lastSchematic.file != null);
@@ -308,7 +309,7 @@ public class DesktopInput extends InputHandler{
 
         if(!locked && block == null && !scene.hasField() && !scene.hasDialog() &&
                 //disable command mode when player unit can boost and command mode binding is the same
-                !(!player.dead() && player.unit().type.canBoost && Binding.commandMode.value.key == Binding.boost.value.key)){
+                !(!player.dead() && player.unit().type.canBoost && Binding.commandMode.value.equals(Binding.boost.value))){
             if(settings.getBool("commandmodehold")){
                 commandMode = input.keyDown(Binding.commandMode);
             }else if(input.keyTap(Binding.commandMode)){
@@ -931,7 +932,7 @@ public class DesktopInput extends InputHandler{
             commandTap(x, y);
         }
 
-        if(button == Binding.commandQueue.value.key){
+        if(button == Binding.commandQueue.value.key && input.matchModifiers(Binding.commandQueue.value.modifiers)){
             commandTap(x, y, true);
         }
 
diff --git a/core/src/mindustry/ui/dialogs/KeybindDialog.java b/core/src/mindustry/ui/dialogs/KeybindDialog.java
index 3e756e8d13f498a9eadaa355ce07b097254e3d70..7890dca51808de5d4c02e924a67af3f30a27ae3b 100644
--- a/core/src/mindustry/ui/dialogs/KeybindDialog.java
+++ b/core/src/mindustry/ui/dialogs/KeybindDialog.java
@@ -95,10 +95,8 @@ public class KeybindDialog extends Dialog{
             if(keybind.defaultValue instanceof Axis){
                 table.add(bundle.get("keybind." + keybind.name + ".name", Strings.capitalize(keybind.name)), Color.white).left().padRight(40).padLeft(8);
 
-                table.labelWrap(() -> {
-                    Axis axis = keybind.value;
-                    return axis.key != null ? axis.key.getName() : axis.min.getName() + " [red]/[] " + axis.max.getName();
-                }).color(Pal.accent).left().minWidth(90).fillX().padRight(20);
+                table.add();
+                table.labelWrap(() -> keyName(keybind.value)).color(Pal.accent).left().minWidth(90).fillX().padRight(20);
 
                 table.button("@settings.rebind", tstyle, () -> {
                     rebindAxis = true;
@@ -107,10 +105,14 @@ public class KeybindDialog extends Dialog{
                 }).size(bw, bh);
             }else{
                 table.add(bundle.get("keybind." + keybind.name + ".name", Strings.capitalize(keybind.name)), Color.white).left().padRight(40).padLeft(8);
-                table.add(keybind.value.key.getName()).update(l -> {
-                    l.setText(keybind.value.key.getName());
+                table.button("=", Styles.logicTogglet, () -> {
+                    keybind.value = new Axis(keybind.value.key, keybind.value.modifiers ^ KeyBind.modifierExact);
+                    keybind.save();
+                }).checked((_b) -> keybind.value.matchExact()).width(18f).padRight(8f).tooltip(Core.bundle.get("mdtx.keybind.exactMatch")).disabled(b -> keybind.value.key == null);
+                table.add(keyName(keybind.value)).update(l -> {
+                    l.setText(keyName(keybind.value));
                     l.setColor(keybind.value.key == KeyCode.unset ? Color.darkGray : Pal.accent);
-                }).color(Pal.accent).left().minWidth(90).padRight(20);
+                }).color(Pal.accent).left().minWidth(90).fillX().padRight(20);
 
                 table.button("@settings.rebind", tstyle, () -> {
                     rebindAxis = false;
@@ -129,6 +131,22 @@ public class KeybindDialog extends Dialog{
         table.button("@settings.reset", Icon.refresh, tstyle, KeyBind::resetAll).minWidth(200f).colspan(5).padTop(4).margin(10f).height(50f).fill();//MDTX: fix colspan for unbind
     }
 
+    public static String keyName(KeyBind.Axis axis){
+        if(axis.key == null){
+            assert axis.min != null && axis.max != null;
+            return axis.min.getName() + " [red]/[] " + axis.max.getName();
+        }
+        if(axis.modifiers == 0 || (axis.modifiers & KeyBind.modifierBit(axis.key)) != 0) return axis.key.getName();
+
+        StringBuilder builder = new StringBuilder();
+        if((axis.modifiers & KeyBind.modifierCtrl) != 0) builder.append("Ctrl+");
+        if((axis.modifiers & KeyBind.modifierAlt) != 0) builder.append("Alt+");
+        if((axis.modifiers & KeyBind.modifierMeta) != 0) builder.append("Win/Cmd+");
+        if((axis.modifiers & KeyBind.modifierShift) != 0) builder.append("Shift+");
+        builder.append(axis.key.getName());
+        return builder.toString();
+    }
+
     void rebind(KeyBind bind, KeyCode newKey){
         if(rebindKey == null) return;
         rebindDialog.hide();
@@ -139,7 +157,7 @@ public class KeybindDialog extends Dialog{
                 bind.value = newKey.axis ? new Axis(newKey) : new Axis(minKey, newKey);
             }
         }else{
-            bind.value = new Axis(newKey);
+            bind.value = new Axis(newKey, input.modifiers());
         }
         bind.save();
 
@@ -160,6 +178,7 @@ public class KeybindDialog extends Dialog{
 
         rebindDialog.titleTable.getCells().first().pad(4);
         rebindDialog.addListener(new InputListener(){
+            private KeyCode pressedKey = null;
             @Override
             public boolean touchDown(InputEvent event, float x, float y, int pointer, KeyCode button){
                 if(Core.app.isAndroid()) return false;
@@ -169,9 +188,16 @@ public class KeybindDialog extends Dialog{
 
             @Override
             public boolean keyDown(InputEvent event, KeyCode keycode){
+                pressedKey = keycode;
+                return true;
+            }
+
+            @Override
+            public boolean keyUp(InputEvent event, KeyCode keycode){
+                if(pressedKey == null || keycode != pressedKey) return false;
                 rebindDialog.hide();
                 rebind(name, keycode);
-                return false;
+                return true;
             }
 
             @Override
